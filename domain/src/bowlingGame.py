class Bowling():

def __init__(self, scorecard):
    self.scorecard = list(score_card) # Convertimos el string score_card en una lista.
    self.score = 0 # 
    self.current_frame = 1
    self.current_roll = 0

def game_score(self):
    for roll in self.score_card:
        if: